#Exercise 1 - person class
This exercise introduces a simple Java class named Person.  
The class contains a name and age, a constructor to initialize them, and a method speak() that prints a greeting message.  
In the Main class, we create two Person objects and call their speak() method.